# requirements.txt

Flask>=2.0
twilio>=7.0  # Or a more specific version if you have one
supabase>=1.0 # Or a more specific version
google-generativeai>=0.3
python-dotenv>=0.19
waitress>=2.0
requests>=2.25 # Often a dependency of other libraries, but good to have if making direct HTTP calls
